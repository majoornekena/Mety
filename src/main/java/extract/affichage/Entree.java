package extract.affichage;

public class Entree {
    public static FrontManager front;

    public static void main(String[] args) {
        // try {
        // Connect.init("postgres", "postgres", "postgres", "hcompta");
        // // Connect.allowSql();
        // Compte ecriture = new Compte();
        // FrontManager front = new FrontManager("Compte", ecriture);
        // Exercice[] liste = (new Exercice()).find(null);
        // front.setCurrentArray(liste);
        // front.setState(EForm.Liste);
        // Entree.front = front;
        // front.show();
        // } catch (Exception e) {
        // e.printStackTrace();
        // }
    }
}
